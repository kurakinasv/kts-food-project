import GlobalStyle, { BackgroundImage } from '@styles/globalStyle';

import Router from './Router';

const App = () => {
  return (
    <>
      <GlobalStyle />
      <BackgroundImage />
      <Router />
    </>
  );
};

export default App;
